import type { SendspinTimeFilter } from "./time-filter";
import type { StateManager } from "./state-manager";
import type { WebSocketManager } from "./websocket-manager";
import type { ServerTime } from "../types";
export declare class TimeSyncManager {
    private wsManager;
    private stateManager;
    private timeFilter;
    private timeSyncBurstActive;
    private timeSyncBurstSentCount;
    private timeSyncInFlightClientTransmitted;
    private timeSyncInFlightTimeout;
    private timeSyncBurstSamples;
    constructor(wsManager: WebSocketManager, stateManager: StateManager, timeFilter: SendspinTimeFilter);
    startAndSchedule(): void;
    private scheduleNextTimeSyncBurstTick;
    private startTimeSyncBurstIfIdle;
    private sendNextTimeSyncBurstProbe;
    private armTimeSyncProbeTimeout;
    private clearTimeSyncProbeTimeout;
    private handleTimeSyncProbeTimeout;
    private finalizeTimeSyncBurst;
    private selectTimeSyncBurstCandidate;
    private abortTimeSyncBurst;
    stop(): void;
    handleServerTime(message: ServerTime): void;
    sendTimeSync(clientTimeUs?: number): number;
}
//# sourceMappingURL=time-sync-manager.d.ts.map