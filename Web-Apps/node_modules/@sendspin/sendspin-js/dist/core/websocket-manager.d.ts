import type { ClientMessage, ReconnectConfig } from "../types";
export declare class WebSocketManager {
    private ws;
    private reconnectTimeout;
    private shouldReconnect;
    private isReconnecting;
    private reconnectAttempt;
    private baseDelayMs;
    private maxDelayMs;
    private maxAttempts;
    private onReconnecting?;
    private onReconnected?;
    private onExhausted?;
    private onOpenHandler?;
    private onMessageHandler?;
    private onErrorHandler?;
    private onCloseHandler?;
    constructor(config?: ReconnectConfig);
    /**
     * Adopt an existing WebSocket connection.
     * The caller is responsible for having already opened the socket.
     * Reconnection is disabled for adopted sockets.
     *
     * Returns a Promise that resolves once the adopted socket is open. Throws
     * synchronously if the socket is already CLOSING or CLOSED.
     */
    adopt(ws: WebSocket, onOpen?: () => void, onMessage?: (event: MessageEvent) => void, onError?: (error: Event) => void, onClose?: () => void): Promise<void>;
    connect(url: string, onOpen?: () => void, onMessage?: (event: MessageEvent) => void, onError?: (error: Event) => void, onClose?: () => void): Promise<void>;
    private openSocket;
    private getReconnectDelayMs;
    private scheduleReconnect;
    private clearReconnectState;
    disconnect(): void;
    send(message: ClientMessage): void;
    isConnected(): boolean;
    getReadyState(): number;
}
//# sourceMappingURL=websocket-manager.d.ts.map