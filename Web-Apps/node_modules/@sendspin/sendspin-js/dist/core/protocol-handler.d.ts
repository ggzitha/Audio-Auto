import type { SendspinTimeFilter } from "./time-filter";
import type { Codec, ControllerCommand, ControllerCommands, GoodbyeReason } from "../types";
import type { StreamHandler } from "../internal-types";
import type { StateManager } from "./state-manager";
import type { WebSocketManager } from "./websocket-manager";
export interface ProtocolHandlerConfig {
    clientName?: string;
    codecs?: Codec[];
    bufferCapacity?: number;
    useHardwareVolume?: boolean;
    onVolumeCommand?: (volume: number, muted: boolean) => void;
    onDelayCommand?: (delayMs: number) => void;
    getExternalVolume?: () => {
        volume: number;
        muted: boolean;
    };
}
export declare class ProtocolHandler {
    private playerId;
    private wsManager;
    private streamHandler;
    private stateManager;
    private timeFilter;
    private clientName;
    private codecs;
    private bufferCapacity;
    private useHardwareVolume;
    private onVolumeCommand?;
    private onDelayCommand?;
    private getExternalVolume?;
    private timeSyncManager;
    constructor(playerId: string, wsManager: WebSocketManager, streamHandler: StreamHandler, stateManager: StateManager, timeFilter: SendspinTimeFilter, config?: ProtocolHandlerConfig);
    handleMessage(event: MessageEvent): void;
    private handleServerMessage;
    private handleServerHello;
    private restartStateUpdateInterval;
    stopTimeSync(): void;
    private handleStreamStart;
    private handleStreamClear;
    private handleStreamEnd;
    private handleServerCommand;
    sendClientHello(): void;
    sendStateUpdate(skipHardwareRead?: boolean): void;
    sendGoodbye(reason: GoodbyeReason): void;
    sendCommand<T extends ControllerCommand>(command: T, params: ControllerCommands[T]): void;
}
//# sourceMappingURL=protocol-handler.d.ts.map