import type { SendspinPlayerConfig, PlayerState, StreamFormat, GoodbyeReason, ControllerCommand, ControllerCommands, CorrectionMode } from "./types";
declare function detectIsAndroid(): boolean;
declare function detectIsIOS(): boolean;
declare function detectIsMobile(): boolean;
declare function detectIsCastRuntime(): boolean;
/**
 * Get platform-specific default static delay in milliseconds.
 * Based on testing across various platforms and browsers.
 */
declare function getDefaultSyncDelay(): number;
export declare class SendspinPlayer {
    private core;
    private scheduler;
    private ownsAudioElement;
    private disconnectPlaybackResetTimeout;
    private suppressDisconnectPlaybackReset;
    constructor(config: SendspinPlayerConfig);
    private cancelPendingDisconnectPlaybackReset;
    private resetPlaybackStateAfterDisconnect;
    private scheduleDisconnectPlaybackReset;
    connect(): Promise<void>;
    /**
     * Disconnect from Sendspin server
     * @param reason - Optional reason for disconnecting (default: 'shutdown')
     */
    disconnect(reason?: GoodbyeReason): void;
    setVolume(volume: number): void;
    setMuted(muted: boolean): void;
    setSyncDelay(delayMs: number): void;
    /**
     * Set the sync correction mode at runtime.
     */
    setCorrectionMode(mode: CorrectionMode): void;
    /**
     * Send a controller command to the server.
     */
    sendCommand<T extends ControllerCommand>(command: T, params: ControllerCommands[T]): void;
    get isPlaying(): boolean;
    get volume(): number;
    get muted(): boolean;
    get playerState(): PlayerState;
    get currentFormat(): StreamFormat | null;
    get isConnected(): boolean;
    get correctionMode(): CorrectionMode;
    get timeSyncInfo(): {
        synced: boolean;
        offset: number;
        error: number;
    };
    /** Get current server time in microseconds using synchronized clock */
    getCurrentServerTimeUs(): number;
    /** Get current track progress with real-time position calculation */
    get trackProgress(): {
        positionMs: number;
        durationMs: number;
        playbackSpeed: number;
    } | null;
    get syncInfo(): {
        clockDriftPercent: number;
        syncErrorMs: number;
        resyncCount: number;
        outputLatencyMs: number;
        playbackRate: number;
        correctionMethod: "none" | "samples" | "rate" | "resync";
        samplesAdjusted: number;
        correctionMode: CorrectionMode;
    };
}
export * from "./types";
export { SendspinTimeFilter } from "./core/time-filter";
export { SendspinCore } from "./core/core";
export { SendspinDecoder } from "./audio/decoder";
export { AudioScheduler } from "./audio/scheduler";
export { detectIsAndroid, detectIsIOS, detectIsMobile, detectIsCastRuntime, getDefaultSyncDelay, };
//# sourceMappingURL=index.d.ts.map