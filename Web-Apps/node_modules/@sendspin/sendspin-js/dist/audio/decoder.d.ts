/**
 * Audio decoder pipeline for Sendspin protocol.
 *
 * Decodes compressed audio (PCM, Opus, FLAC) into raw Float32Array PCM samples.
 * This module has no Web Audio playback concerns — it only produces decoded data.
 */
import type { StreamFormat, DecodedAudioChunk } from "../types";
export declare class SendspinDecoder {
    private onDecodedChunk;
    private currentGeneration;
    private webCodecsDecoder;
    private webCodecsDecoderReady;
    private webCodecsFormat;
    private useNativeOpus;
    private nativeDecoderQueue;
    private opusDecoder;
    private opusDecoderModule;
    private opusDecoderReady;
    private flacDecodingContext;
    private flacDecodingContextSampleRate;
    private flacDecodingContextChannels;
    constructor(onDecodedChunk: (chunk: DecodedAudioChunk) => void, currentGeneration: () => number);
    /**
     * Handle a binary audio message from the WebSocket.
     * Parses the message, decodes the audio, and emits a DecodedAudioChunk.
     */
    handleBinaryMessage(data: ArrayBuffer, format: StreamFormat, generation: number): Promise<void>;
    private decode;
    private decodePCM;
    private getFlacDecodingContext;
    private decodeFLAC;
    private initWebCodecsDecoder;
    private createWebCodecsDecoder;
    private handleAudioData;
    private emitDeinterleavedChunk;
    private queueToNativeOpusDecoder;
    private resolveOpusDecoderModule;
    private resolveOggOpusDecoderClass;
    private waitForOpusReady;
    private initOpusEncdecDecoder;
    private decodeOpusWithEncdec;
    /** Clear decoder state (on stream change/clear). Drops in-flight async decodes. */
    clearState(): void;
    /** Full cleanup (on disconnect). Releases all decoder resources. */
    close(): void;
}
//# sourceMappingURL=decoder.d.ts.map